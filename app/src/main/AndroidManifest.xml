<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RefriMancia">
        <activity

            android:name=".LoginActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateRecipeActivity" />

        <activity android:name=".VentanaRegistro" />
        <activity android:name=".VentanaVerificar" />
        <activity android:name=".VentanaRecuperarPassword" />
        <activity android:name=".ui.ContenedorPrincipalActivity" />
        <activity android:name=".ui.RecetaActivity" />

      <!-- Actividad de recorte de uCrop con el nuevo tema corregido -->
      <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.UCrop"/>

        <!-- 345: This activity is for testing purposes and can be removed. -->
        <activity android:name=".ExampleActivity" />
    </application>
</manifest>

