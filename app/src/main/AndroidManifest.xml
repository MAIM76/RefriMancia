<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- android:icon="@mipmap/ic_launcher" -->
    <!-- android:roundIcon="@mipmap/ic_launcher_round" -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/login_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/login_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.RefriMancia">
        <activity

            android:name=".ui.LoginActivity"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.CreateRecipeActivity" />

        <activity android:name=".ui.VentanaRegistro" />
        <activity android:name=".ui.VentanaVerificar" />
        <activity android:name=".ui.VentanaRecuperarPassword" />

        <activity android:name=".ui.VentanaEditarPerfil" />

        <activity
            android:name=".ui.pablo.ContenedorPrincipalActivity"
            android:theme="@style/Theme.RefriMancia.Contenedor"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".ui.pablo.RecetaActivity" />


      <!-- Actividad de recorte de uCrop con el nuevo tema corregido -->
      <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.UCrop"/>

    </application>
</manifest>
